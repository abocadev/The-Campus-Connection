<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, shrink-to-fit=no">
    <title>Inicia sesión</title>
    <link rel="stylesheet" href="{{ asset('css/bootstrap.css') }}">
    <script src="{{ asset('js/bootstrap.js') }}"></script>

    <style>
        .mh-100vh {
            min-height: 100vh;
        }

        #login-block {
            -webkit-box-shadow: 0px 0px 45px 0px rgba(0, 0, 0, 0.4);
            -moz-box-shadow: 0px 0px 45px 0px rgba(0, 0, 0, 0.4);
            box-shadow: 0px 0px 45px 0px rgba(0, 0, 0, 0.4);
            z-index: 2;
        }

        @media (max-width: 991.98px) {
            #login-block {
                opacity: .95;
            }
        }

        @media (max-width: 991.98px) {
            #bg-block {
                position: fixed;
                top: 0;
                left: 0;
                bottom: 0;
                right: 0;
            }
        }

        @media (min-width: 992px) {
            .w-lg-75 {
                width: 75%;
            }
        }

        @media (min-width: 992px) {
            .rounded-lg-0 {
                border-radius: 0 !important;
            }
        }

        @media (min-width: 1200px) {
            .w-xl-50 {
                width: 50%;
            }
        }
    </style>
</head>

<body>
<div class="container-fluid">
    <div class="row mh-100vh">
        <div class="col-10 col-sm-8 col-md-6 col-lg-6 offset-1 offset-sm-2 offset-md-3 offset-lg-0 align-self-center d-lg-flex align-items-lg-center align-self-lg-stretch bg-white p-5 rounded rounded-lg-0 my-5 my-lg-0"
             id="login-block" style="background: #3c3c3c !important;">
            <div class="m-auto w-lg-75 w-xl-50">
                <picture>
                    <img style="display: block;margin: 0 auto;width: 8rem;height: 8rem;" src="{{ asset('img/Logo.png') }}">
                </picture>
                <h2 class="text-info fw-light mb-5"
                    style="color: var(--bs-body-bg)!important;text-align: center; text-size:2rem;">The Campus
                    Connection</h2>
                <form action="{{ path('app_login') }}" method="post">
                    {% if error %}
                        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                    {% endif %}
                    <div class="form-group mb-3"><label class="form-label text-secondary" style="color: rgb(255,255,255)!important;">Email</label>
                        <input type="text" id="username" name="_username" value="{{ last_username }}" class="form-control" required="" pattern="[a-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,15}$" inputmode="email"></div>
                    <div class="form-group mb-3">
                        <label class="form-label text-secondary" style="color: rgb(255,255,255)!important;">Password</label>
                        <input type="password" id="password" name="_password" class="form-control" required="">
                    </div>
                <button class="btn btn-primary" type="submit" style="display:block; margin:0 auto;">Acceder</button>
                </form >
                <p class="mt-3 mb-0" style="color: var(--bs-body-bg)!important;text-align: center; text-size:2rem;">
                    <a class="text-info small" href="{{ path('app_registration')}}" style="color: var(--bs-body-bg)!important;text-align: center; text-size:2rem;">No tienes cuenta? Haz click aquí</a>
                </p>
            </div>
        </div>
        <div class="col-lg-6 d-flex align-items-end" id="bg-block" style="background: url('{{ asset('img/fondo_login.jpg') }}') center center / cover;">
            <p class="ms-auto small text-dark mb-2"><em>Photo&nbsp;</em>
                <a class="text-dark" href="https://www.wallpaperflare.com/table-work-computer-study-reading-technology-connection-wallpaper-gmsqw" target="_blank"><em>source</em></a><br></p>
        </div>
    </div>
</div>
</body>
</html>